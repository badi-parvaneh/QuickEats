<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a160final">

    <!--
    <application
        android:name="com.example.a160final.UserGlobal"
        android:allowBackup="true"
        android:label="xyz"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">
    </application>
    -->

    <application
        android:name=".UserGlobal"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeScreenMenu"
            android:label="@string/title_activity_home_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CartActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".ConfirmationActivity" />
        <activity android:name=".CheckStatusActivity" />
        <activity android:name=".OrderHistoryActivity"></activity>
        <activity android:name=".CreditCardActivity"></activity>
        <activity android:name=".PaymentActivity"></activity>
        <activity android:name=".SignUp"></activity>
        <activity android:name=".HomeScreen"></activity>
        <activity android:name=".settingActivity"></activity>
        <activity android:name=".CategoryRestaurantPage"></activity>
        <activity android:name=".RestaurantPage"></activity>
        <activity android:name=".BillingAddressActivity"></activity>

    </application>

</manifest>